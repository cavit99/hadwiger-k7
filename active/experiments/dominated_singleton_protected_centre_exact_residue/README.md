# Historical protected-centre residue profile

This is an unaudited discovery diagnostic for the former exact
eight-terminal catalogue.  No output is a promoted theorem.  The current
route instead protects two centres in one common contraction and uses the
[audited order-nine/ten reduction](../../hc7_k7minus_dominated_two_protected_centres_kernel.md).

Run from the repository root:

```text
python3 active/experiments/dominated_singleton_protected_centre_exact_residue/probe.py
```
